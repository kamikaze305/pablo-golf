interface GameFooterProps {
  // Add any footer-specific props here if needed in the future
}

export function GameFooter({}: GameFooterProps) {
  return (
    <div className="mt-8 text-center text-gray-500 text-sm">
    </div>
  );
}
